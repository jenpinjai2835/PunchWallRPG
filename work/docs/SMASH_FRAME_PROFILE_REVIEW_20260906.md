# Frame profiler readiness review

Agent 2, 2026-09-06. Read-only review of the Coordinator's `work/tools/smash-frame-profile.mjs` against the current source in `F:/Roblox/PuchWall-completion-20260906`. Reviewed helper SHA256: `6891275f0c97068e7166e9015768723ca5c6813ed0f91474f04aa829e576133c`; repository HEAD observed: `6211b0075df92b38e0b5c13f4824968909a06b10`. This review changes only this document. The helper was not executed, Studio was not accessed, and no measured frame-rate result is available.

The helper is **BLOCKED for interpretable final-artifact measurement until the following small tooling fixes are verified**. These are profiler issues, not newly demonstrated gameplay defects. Findings were sent to the Coordinator during review; the Coordinator owns helper changes.

| Priority | Finding and exact source basis | Required correction |
| --- | --- | --- |
| P2 | Helper lines 8–11 record the current repository commit and an artifact hash, then select an explicit Studio UUID without comparing its contents to that artifact. A correct disk hash cannot establish which source the selected Studio is executing. | Run only after rebuilt-artifact reopen verification; bind that verified evidence, exact UUID/place identity, artifact hash, and all nine current source hashes. `smash-live-source-check.mjs` already provides exact normalized source comparisons. Name the Git value `repositoryCommit` unless it is specifically the artifact's recorded build commit. |
| P2 | Helper lines 20–22 disconnect the RenderStepped listener only on success. An intermediate exception leaves the watcher until Play stops; start/stop tool errors are ignored. Artifact hashing occurs after the MCP child is spawned but outside `try`; evidence writing occurs before stop/close in `finally`. A hash or write exception can therefore skip cleanup. | Validate files before spawning; protect client lifetime; attempt profiler Destroy and Play stop in cleanup independently of evidence writing; record both results, check `isError`, and exit nonzero on failure. Preserve the primary error if cleanup also fails. |
| P2 | Helper lines 12, 17–19 ignore Reset/SetStats and navigation results, and immediately trust phase labels. OpenInventory can return `true` without a controller; an unavailable category command can return false. The server's `SetStats` reports ok even when `setStat` finds no matching value (`PunchWallBootstrap.server.lua:2620`, `:9626`). | Check returned shapes and actual seeded stats. Assert client Snapshot Shop/Inventory visibility and requested category; require the observed seeded inventory before measurement. Include `PersistenceStudioDefaultEphemeral==true` to match the existing strict fixture proof. |
| P2 | Helper line 20 invokes `StressPunchCase`, whose first cycle calls `resetAutomationState`, and whose subsequent cycles reset the world (`PunchWallBootstrap.server.lua:9395`). Reset clears seeded fists, pets and equipment; the case adds four teleports and direct server punches plus collision diagnostics. | Label this phase as server stress including resets, teleports, destruction and harness work. It cannot establish ordinary client-input punch performance or performance with the previously seeded companions. Use a separate actual client-punch phase if that claim is required. |
| P3 | Helper line 13 reads `TargetDepthQueryCount`, `TargetDepthCandidateCount` and `TargetDepthUniqueCandidates` once at Finish. The producer replaces these values on each selector call (`PunchWallClient.client.lua:9279`); they are not cumulative. | Nest or label them as `lastTargetScan`, or collect an explicitly bounded phase distribution. Do not present the final values as phase totals or maxima. |
| P3 | Phases start and finish through separate MCP calls. The first/last frame intervals include command boundary and transport scheduling. Inventory automation also computes full diagnostic snapshots that native category callbacks skip (`InventoryUI.lua:3432`, `:3500`). | State that these are Studio RenderStepped intervals including MCP/harness work. Record phase duration, sample count, and relevant command boundaries. The 36,000-sample cap is bounded; record saturation/dropped samples so a partial interval cannot silently represent an entire phase. |

Command and data checks against the actual producers:

| Call | Verified contract |
| --- | --- |
| `selectStudioStrict(c,{studioInstanceId:id})` | Exact UUID matching and explicit tool routing are supported. It does not independently bind the artifact file. |
| `execute_luau({datamodel_type:'Server'/'Client',code})` | Matches `McpClient.callTool`; `text` is the joined MCP text content and `isError` covers protocol/tool errors. JSONEncode results can be parsed as the helper does. |
| Server `Reset` / `SetStats`, table payload | Supported; returns a snapshot with `ok`. Reset is destructive to fixture stats/world and must remain behind the ephemeral proof. The existing non-writable/ready/Studio/live-opt-in checks are valid; adding the default-ephemeral attribute completes parity with the strict fixture. |
| Client `OpenTab,'Fists'` / `CloseMenus` | Supported; returns true. Use Snapshot to prove the resulting visibility rather than trusting the acknowledgement alone. |
| Client `OpenInventory` / `SelectInventoryCategory,category` | Supported. Categories All/Fists/Pets/Honor are valid; Boosts also exists. Normal returns are snapshot tables containing `ok`, `visible`, `category`, `visibleNames`, and layout. |
| Server `StressPunchCase`, `{name,x,power,cycles=4,attempts=5,resetTotal=true,expectedTotal=20}` | Supported; bounds permit 4×5. Return fields `valid`, `punches`, `total`, `overlaps`, `stuck` are real. Its `valid` additionally checks success rate, correction/backward movement, humanoid state and endpoint mode. |

All five fist identifiers match the ordinary catalog: Starter Glove, Boxing Glove, Iron Knuckle, Thunder Fist, Titan Gauntlet (`GameConfig.lua:245`). Pet tokens Forest Pup and Miner Cat are valid (`:417`, `:419`). The initial helper seeds three inventory copies but only two distinct species and two equipped pets, not three companion models. `MaxEquippedPets` is three. If three equipped species are intended, Forest Pup, Meadow Bunny, and Miner Cat are all valid catalog names; verify the resulting equipped instances rather than changing only the evidence label.

The percentile algorithm uses sorted frame deltas and the nearest-rank index `ceil(n*q)`; p50/p95/p99 and maximum therefore have consistent units in milliseconds. Counts over 50/100 ms are frame counts, not time percentages. Memory values are total client-reported usage at phase boundaries, not allocations attributable to the game or proof of a leak. The existing “Studio frame intervals, not physical phone performance” label is appropriate and must remain. No FPS threshold or physical-phone claim is supported by this unexecuted helper.

Validation performed: JavaScript `node --check` PASS; command names, payloads, return shapes, catalog identities, producer counters, phase effects and cleanup paths inspected against actual source. Re-review of the revised helper, strict artifact identity verification, and actual execution remain pending the Coordinator. Measurement must occur only after the rebuilt artifact is reopened and verified; the frozen full regression remains independent of this review.
