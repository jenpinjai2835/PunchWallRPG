# Source frame diagnosis and execution context — 2026-09-06

Agent 1. Gameplay source is read-only; the authorized follow-up changes only this document and `work/automation/scripts/studio-frame-profile.mjs`. The initial two records run immutable `adba937ed3a9c80f74b323735f1463e1683e3df5`; its client normalized SHA256 is `76cce1937f0ef88269e3fc42b09b8bf3432622574169a18c9c8ef121fa5bd6f1`. The third record runs `078ca5c6a81a03713dbfdbc3fffe78acab15438f`, client normalized SHA256 `3301d52e644cf756f01db0038d6a1f9a7b953f23afcb08ca7c13f3bd3b44a8ae`. Its 72 added lines are Studio camera-test diagnostics in `__RunCamera`; that routine is unused by the idle/Shop profile. No Studio, gameplay source, flow, artifact binding, or registry changes were made by this assignment.

## Finding: retain the measured improvement, correct the comparison

The initial two records show substantially shorter frame intervals after native Studio activation, but **the recorded viewport also changed**. The third run, after the Coordinator explicitly clicked Maximize and observed the resulting Restore control, reproduces short intervals at the original 1277 × 780 viewport. This supports a testing-context explanation without establishing an engine background cap or a specific game bottleneck. No gameplay performance fix occurred between these measurements.

| Recorded context / phase | Viewport | Intervals / seconds | p50 ms | p95 ms | Maximum ms | Over 50 ms |
| --- | --- | --- | --- | --- | --- | --- |
| Earlier / idle | 1277 × 780 | 92 / 6.1353 | 66.799 | 71.165 | 76.535 | 92 |
| Earlier / Shop | 1277 × 780 | 82 / 6.1302 | 69.751 | 100.077 | 120.983 | 82 |
| After activation / idle | 637 × 654 | 368 / 6.1599 | 16.667 | 21.827 | 36.036 | 0 |
| After activation / Shop | 637 × 654 | 366 / 6.1175 | 16.762 | 21.680 | 33.319 | 0 |
| Explicitly maximized / idle | 1277 × 780 | 369 / 6.1539 | 16.668 | 21.090 | 31.912 | 0 |
| Explicitly maximized / Shop | 1277 × 780 | 368 / 6.1281 | 16.540 | 21.729 | 34.883 | 0 |

Evidence lives in `work/docs/evidence/smash-source-idle-shop-profile-20260906.json` and `smash-source-idle-shop-profile-foreground-20260906.json`. The third evidence file is `smash-source-idle-shop-profile-maximized-20260906.json`. Both `profiler.viewport` and `client.closed.viewport` independently confirm the sizes in the table. All three files attest their nine exact source objects before/after, the same Studio instance, Automatic graphics, and two seeded normal companions. The first two share a source commit; the third source difference is limited to the unused Studio diagnostic routine described above. All record zero invalid/dropped samples and successful cleanup. Their final target-selector snapshots are identically three queries and 72 unique candidates, but those are not phase totals, maxima, or CPU timings.

The earlier record ran at 00:42:27–00:43:28 UTC; the later record at 00:45:18–00:46:21 UTC. The third record ran at 00:54:27–00:55:24 UTC. Native activation/maximization is Coordinator-observed context; these earlier JSON records do not continuously attest native foreground state. Automatic is the same setting enum, not proof that identical effective graphics quality or GPU workload persisted. The changed viewport makes that distinction material.

## Source and API interpretation

The collector in `work/automation/scripts/studio-frame-profile.mjs` records client `RenderStepped` intervals in milliseconds, using bounded storage and nearest-rank percentiles. It disables the phase before sorting and records elapsed time, invalid/dropped counts, and explicit cleanup. Separate MCP Begin/Finish boundaries and diagnostic navigation remain part of the measured workload. A 16.667 ms median describes the observed interval distribution; it is not script CPU duration, GPU duration, or an assurance that every frame met that budget. [Roblox RunService reference](https://create.roblox.com/docs/reference/engine/classes/RunService#RenderStepped).

The evidence source has no WindowFocused/WindowFocusReleased branch or explicit frame-rate setting that explains the difference. The existing camera suspended-render fallback activates only after 0.35 seconds without render callbacks; the observed 0.067-second median alone does not establish that it was active. Target selection is scheduled at 0.15-second intervals with cached folders and progressive complete spatial searches. Companion updates use distance-based intervals and existing model state. Shop refresh returns early for an unchanged relevant-state signature, with boost countdown scheduling separate from catalog reconstruction. These facts describe the implementation; this sampler does not time those functions or establish their individual cost. No speculative gameplay optimization is justified from these three records.

Roblox explicitly cautions that Studio performance stats are affected by the Studio application and recommends the client for accurate frame-rate measurements. Its guidance also notes device differences and that Automatic graphics can affect comparisons. A controlled desktop source measurement remains distinct from physical-phone, native-input combat, multiplayer, and rebuilt-artifact validation. [Identify performance issues](https://create.roblox.com/docs/performance-optimization/identify).

The roughly 4.3–4.6 GB boundary readings cannot be attributed to a game leak. `GetTotalMemoryUsageMb()` reports operating-system-derived total session memory and can differ from the sum of developer memory categories. The short earlier Shop sample even decreased from 4553.652 to 4332.859 MB; neither a decrease nor a small increase establishes a lifetime trend or allocation owner. [Stats memory API](https://create.roblox.com/docs/reference/engine/classes/Stats/GetTotalMemoryUsageMb).

## Minimal final-profile context requirement

The reviewed tooling gap is corrected: the final profiler previously recorded one setup viewport and the generic label “Studio desktop window,” with no focus observation or per-phase resize record. Final execution instructions should require the Coordinator to activate the exact verified Studio instance, settle its viewport, and preserve an explicit native foreground identity/timestamp. The evidence should retain each phase's viewport start/end plus observed resize/focus transitions. Do not label a phase as continuously foreground or fixed-size when that state is unknown or changed. Preserve the existing raw samples and limitations rather than silently dropping inconvenient frames.

The collector now records viewport start/end, viewport/camera change totals, and up to 12 transition records per phase. Every property event invalidates the phase even when deferred delivery observes an already-restored viewport/camera; Finish additionally rechecks actual dimensions. Invalid dimensions, any such change, or any focus transition marks `contextValid=false`. The runner retains raw phase metrics before rejecting that result. Focus starts as `unknown`, and only actual `UserInputService.WindowFocused` / `WindowFocusReleased` events change it; `focusStart`, `focusEnd`, `focusKnownWholePhase`, totals, and a bounded trace preserve that distinction. A stable unknown or unfocused phase is explicitly labeled as such and cannot establish foreground execution; native identity/activation evidence remains separate. All render, camera, workspace, and focus listeners disconnect on explicit, external-destruction, or deadline cleanup. These documented transition events do not supply an initial-state getter. [UserInputService focus events](https://create.roblox.com/docs/reference/engine/classes/UserInputService#WindowFocused). Artifact preflight and the exact nine-source binding remain byte-identical to the reviewed version.

First run the intended final profile with verified foreground and stable viewport. Only if a slow interval remains reproducible in that context is a bounded MicroProfiler capture or sampling of `Stats.RenderCPUFrameTime` / `RenderGPUFrameTime` warranted to distinguish rendering work from other frame time. Those probes would guide a specific fix; they are not required to explain an unsupported background-cap theory. [MicroProfiler diagnostic workflow](https://create.roblox.com/docs/performance-optimization/microprofiler/use-microprofiler), [Stats render timing](https://create.roblox.com/docs/reference/engine/classes/Stats#RenderCPUFrameTime).

Validation: parsed all three actual JSON records and all six nine-source checks; verified measured-source hashes and the third source diff; inspected collector/source paths; consulted official Roblox documentation. `node --check` passes. The updated profiler self-test passes 66 Node checks, eight production snippet compilations, and 53 assertions executing the exact guard/collector. New controls cover unchanged/changed/restored/unannounced/nonfinite viewports, same-size camera replacement and return, initial unknown focus, observed focus persistence/loss/restoration, bounded traces, and every listener cleanup path. Seven compiling mutations that trust only the current value after deferred delivery, ignore resize events, fabricate initial foreground, waive viewport/focus gates, leak context listeners, or remove the trace cap fail intentionally. Those self-tests are not performance measurements. No new P1/P2 gameplay-source defect is established. Controlled final-context, artifact-bound and physical-device performance remain separate pending checks.

Checklist: source/evidence/API inspection complete; viewport confound reported promptly and fixed-viewport follow-up recorded; bounded context collector and negative controls complete; focused offline checks and diff review complete; eligible for Coordinator integration and artifact-bound final execution.
